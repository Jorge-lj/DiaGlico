<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/gradient_fundo"
    android:padding="16dp"
    tools:context=".view.MainActivity">

    <!-- Saudação -->
    <TextView
        android:id="@+id/textViewNomeUsuario"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="Olá, Usuário!"
        android:textColor="#DBB7B7"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Título -->
    <TextView
        android:id="@+id/textViewTituloGlicemias"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:text="Últimas Glicemias"
        android:textColor="#DBB7B7"
        android:textSize="20sp"
        android:textStyle="italic" />

    <!-- Lista -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewGlicemias"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:scrollbars="vertical"
        tools:listitem="@layout/activity_item_glicemia" />

    <!-- FABs -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="8dp">

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnNovoRegistro"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|start"
            android:src="@android:drawable/ic_input_add"
            app:backgroundTint="#190202"
            app:tint="#FFFFFF"
            android:contentDescription="Adicionar novo registro" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnVerPerfil"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:src="@android:drawable/ic_menu_myplaces"
            app:backgroundTint="#190202"
            app:tint="#FFFFFF"
            android:contentDescription="Perfil de Usuário" />

    </FrameLayout>
</LinearLayout>
